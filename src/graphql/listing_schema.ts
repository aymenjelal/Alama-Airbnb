import { gql } from 'apollo-server-express';

export const types = `
  type listing {
    id: String
    name: String
  }
`;

export const inputs = `
  
`;

export const queries = `

`;

export const mutations = `


`;
